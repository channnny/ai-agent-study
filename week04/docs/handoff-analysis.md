# 3주차 설계서 핸드오프 검토 — 이지현·임지현 비교 분석

> 작성일: 2026-05-19 (W04 시점)
> 입력: 이지현 handoff 패키지 + 임지현 `agent_design_v0.4.md` + 유찬 week03 산출물(POC 크롤러 + 메타 레이어) + Notion 조교ON W02/W03 회의록
> 목적: 4주차 크롤링 테스트 구현 시 두 설계서의 장단점과 누락 영역을 파악하고, 무엇을 통합·보완할지 정리.

---

## 0. 한 줄 요약

| 사람 | 한 줄 평가 |
|---|---|
| **이지현** | 광범위(4시트 전체 커버) + 도메인 지식 풍부. **거의 SDD 수준**의 완성도. |
| **임지현** | POC 실측 11건 반영 + 운영 디테일 강함. **시행착오로 검증된 v0.4**. |
| **유찬(본인)** | **POC 크롤러(212L) + 메타·통합·인프라 레이어** — `crawl_adiga.py`로 전형 결과 추출 작동 확인 (이지현 정적 HTML 가설 입증) + Notion MCP 인프라 + CLAUDE.md standing instructions + 본 비교 분석 + W04 실행 플랜. |

**결론**: 세 사람의 기여는 **서로 다른 추상 레이어**에서 보완적이다. 이지현(설계 — 범위/스키마/평가체계) × 임지현(POC 검증 — 평면화/부트스트랩/에러 격리) × 유찬(메타 — 통합 분석/시스템 컨텍스트/실행 플랜 + 부분 POC). §1 매트릭스의 유찬 컬럼은 각 영역에 대한 **통합·의사결정 입장**(주로 "이지현 X / 임지현 Y 채택 권장")이며, 본인이 직접 설계한 산출물 5종 상세는 §4 참조.

---

## 1. 비교 매트릭스 (한눈에 보기)

| 영역 | 이지현 | 임지현 | 유찬 |
|---|---|---|---|
| **산출물 분량** | README + agent_design(739L) + 8대학 분석 + 정규화 사전 + PoC 코드 3종 + 골든셋 + schema yaml | 단일 파일 `agent_design_v0.4.md` (315L) | **POC 크롤러 + 메타 레이어 5종** — `crawl_adiga.py` (212L, requests+BS4 정적 크롤러) + `target_universities.csv` (221대학 마스터) + `integration-context.md` + `handoff-analysis.md` (191L) + 프로젝트 CLAUDE.md (164L) + Notion MCP 인프라 |
| **데이터 범위** | 수시/정시 × 결과/평가기준 **4시트 모두** | **수시 단일 시트**만 (정시·평가기준 범위 외) | MVP 수시 단일 시트(임지현) → W05+ 4시트 확장(이지현) 결합 제안 |
| **워크플로우** | T0~T7, 8단계 명세 | 4에이전트(Crawler/Mapper/Validator/Writer) + Orchestrator | 6단계 액션 플랜 (가천대 1건 → 8건 → 골든셋 평가) |
| **검증/실패 처리** | 검증 매트릭스 + 5종 정책(재시도/에스컬레이션/스킵/종료/부분저장) | **fatal vs transient 분류**, 표·대학 단위 격리, **빈 산출 하드 가드 3종** | 임지현 정책 채택 권장 (fatal/transient 분류 + 표·대학 단위 격리 + 빈 산출 가드 3종) |
| **LLM 호출 전략** | Haiku→Sonnet→Opus 에스컬레이션, 호출 상한 5회/대학, 토큰 8000자 상한 | **부트스트랩(100% LLM) ↔ 정상(사전+LLM)** 모드, 인증/잔액 오류 즉시 중단 | MVP는 임지현 Sonnet 단일, W06+ 이지현 Haiku→Sonnet→Opus 에스컬레이션 도입 권장 |
| **표 파싱 디테일** | "`<table>` 추출 + PK 단위 행 변환" 수준 | **rowspan/colspan 평면화** 명세 ("학생부등급.70%cut"식 점결합), forward-fill | POC에서 단순 td/th 추출 + padding으로 전형결과 작동 확인. 운영 품질용으로 임지현 평면화 추가 채택 권장. |
| **학년도 모델** | `searchSyr=2027 → 2026학년도 평가 + 2025년 결과` (도메인 지식) | **`page_year` vs `result_year` 스키마 분리** (POC #8 반영) | 임지현 `page_year`/`result_year` 분리 채택 권장 |
| **URL 패턴** | `univDetailSelection.do?menuId=PCUVTINF2000&unvCd={code}&searchSyr={year}` | **`univDetail.do` 진입 → 메뉴 클릭 후 `univDetailSelection.do` 전환** (POC #5 정정) + `univInfo.do` 대학명→코드 API | **본인 POC 실측 검증** — `univDetailSelection.do` 직접 GET 작동 (이지현 패턴 일치, 임지현 메뉴 클릭 주장과 다름) |
| **도메인 지식 자료** | 8개 대학 사전 분석(가천·서울·제주·연세·고려·부산·경북·남서울) + 정규화 사전 9섹션 | POC 시행착오 11건 명시 | 두 사람 자료(이지현 8대학 분석 + 정규화 사전 9섹션 + 임지현 POC 11건)를 LLM 컨텍스트로 큐레이션 |
| **PK 정의** | (대학, 전형, 모집단위) 3-튜플, 부산대 사례로 검증 | (unvCd + admission_type + recruitment_unit) | 이지현 3-튜플 채택 ((대학, 전형, 모집단위) — 임지현 정의와 일치) |
| **평가/골든셋 비교** | `evaluator` 스킬 + `evaluation_report.xlsx` 6시트, DoD에 PK 매칭률 ≥ 85% / 셀 일치율 ≥ 90% | **없음** | 이지현 `evaluator` + DoD 임계치(PK 매칭률 ≥ 85% / 셀 일치율 ≥ 90%) MVP 채택 |
| **확장 계획** | 8개 → 200+ 대학, partial 모드, `run_state.json` 기반 재개 | 60개 대학 1회 단위, 7일 raw 캐시, `--refresh` | MVP 8개 → W05+ 200+ (이지현 partial 모드 + 임지현 `--refresh`) 단계화 |
| **비용 추정** | LLM 호출 상한만 명시 | **부트스트랩 ~$9/60대학, 정상 ~$2/60대학 구체 수치** | 임지현 추정 채택 + MCP 활용으로 컨텍스트 토큰 약 30배 절약 추가 레이어 |
| **POC 검증 여부** | PoC 코드 3종 첨부, "가천대 76% PK 매칭" 등 일부 수치 | **11건 시행착오 명시** (사전 28개 키만으로 235 records 100% 매핑 등 실측) | **부분 수행** — `crawl_adiga.py` 212L (전형 결과 추출 + 멀티스레드 배치). 221개 대학 마스터 준비. 헤더 평면화·운영 가드·전형별 주요사항 미진행. |

---

## 2. 이지현 — 장단점

### 2.1 강점

1. **범위가 넓다** — 4시트(susi_result / susi_eval / jeongsi_result / jeongsi_eval) 전체를 커버. 임지현은 수시 결과만 다룸.
2. **SDD 완성도** — `agent_design.md` §1~§8 + 부록 ABC. 각 단계마다 **수행 주체/입력/처리/성공 기준/검증 방법/실패 시** 6요소 표준화. 신입 엔지니어가 보고 그대로 구현 가능한 수준.
3. **8개 대학 사전 분석 첨부** — `00_통합_발견.md`에 5종 평가요소 표기, 면접 4유형, PK 분기(부산대 케이스), 캠퍼스 매핑이 정리됨. **normalizer-eval 서브에이전트의 도메인 컨텍스트**로 그대로 활용 가능.
4. **정규화 사전 분리** (`normalization-dictionary.md` 9섹션) — 반영교과/등급기준/NULL/숫자/Boolean/전형구분/면접유형/평가표기방식/캠퍼스. 룰 기반 매핑이 LLM 부담 줄이는 구조.
5. **골든셋 평가 체계** — `evaluator` 스킬 + 6시트 리포트(summary, by_university, by_column, missing_rows, extra_rows, mismatched_cells) + DoD 임계치(PK 85%, 셀 90%). 측정 가능한 완료 정의.
6. **스킬 vs 서브에이전트 분리** — Claude Code의 SKILL.md / AGENT.md 패턴 적극 활용. 결정론은 스킬, 판단은 서브에이전트.
7. **실행 가능한 자산 첨부**: `schema_v3.yaml`, `universities.csv`, `2025_어디가입결_통합본.xlsx` (골든셋 21,435행).
8. **유찬 POC로 핵심 가설 입증** — "정적 HTML, requests+BS4 OK"가 실제 동작 확인됨.

### 2.2 약점·누락

1. **W04 "굴러가게만" 목표 대비 과욕** — 4시트 + 8개 대학 + 골든셋 평가 + 200+ 확장 + 서브에이전트 분리까지. 1주차로 보기엔 범위가 크고, 부분 구현 시 측정이 어렵다.
2. **표 파싱 디테일이 추상적** — "BeautifulSoup으로 HTML 파싱 → 4시트 raw DataFrame"이라고만 함. **rowspan/colspan 처리, 그룹 헤더 평면화, forward-fill 같은 실제 어려움은 명세 없음** (임지현 §3.2가 정확히 이 부분).
3. **학년도 의미가 도메인 지식으로만 존재** — `searchSyr=2027 → 2026 평가 + 2025 결과` 매핑은 부록에 있으나, **스키마에 `page_year` vs `result_year` 분리는 없음**. 운영 중 혼동 위험 (임지현이 POC #8로 잡아낸 부분).
4. **빈 산출 가드 약함** — "충진율 ≥ 60%" 같은 임계치는 있으나, `records == 0`이 PASS로 흐를 가능성에 대한 **하드 가드는 명시 없음** (임지현 §3.4의 핵심).
5. **인증/잔액 오류 분류 부재** — "LLM 응답 파싱 실패 → 재시도 → raw_text 보관"까지만. **fatal vs transient 분류, 인증/잔액 오류는 즉시 중단** 같은 운영 정책 없음 (임지현 §3.3 + POC #10).
6. **부트스트랩 모드 부재** — 매핑 사전이 있다고 가정. 사전이 빈 상태에서 시작하는 cold-start 시나리오에 대한 단계화 없음 (임지현 §3.3).
7. **PoC 코드의 검증 결과 부분적** — "가천대 76% PK 매칭"은 언급되나, 어디서 실패했는지, 어떻게 개선해야 하는지 시행착오 기록 없음. handoff README는 "PoC 그대로 사용 금지"라고만.

---

## 3. 임지현 — 장단점

### 3.1 강점

1. **POC로 검증된 v0.4** — §10에 시행착오 11건 명시. 추측이 아니라 **실측 기반**.
2. **헤더 평면화 명세** — `<thead>` + `<rowspan>/<colspan>` + 숫자 비율 5% 미만 행 = 헤더 식별 + 그룹 헤더 점결합(`학생부등급.70%cut`). 어디가 표가 깨지는 가장 자주 발생하는 지점을 직격.
3. **부트스트랩 ↔ 정상 모드** — 사전 항목 < 20이면 부트스트랩(100% LLM, 첫 5개 강제 REVIEW)으로 시드 확보. 정상 모드 전환 임계 + 자동 통과율 ≥ 70% 함께 명시. **cold-start 문제 해결**.
4. **빈 산출 하드 가드 3종** — records=0, records < raw 행수 30%, admission_type 한쪽만 잡힘 — 셋 중 하나라도 걸리면 무조건 REVIEW. Validator 통과해도 별도 가드.
5. **fatal vs transient 에러 분류** — `429`/`5xx`/네트워크는 지수 백오프 (1s→4s→16s), **`AuthenticationError`/`PermissionDeniedError`/`NotFoundError`는 즉시 전체 중단**. POC #10에서 SDK가 잔액 부족을 AuthenticationError로 분류한다는 실측 반영.
6. **대학 단위 게이트** — 한 대학의 LLM 실패율 ≥ 70%면 대학 전체 REVIEW 큐로. 한 대학의 부적합이 전체 배치를 망가뜨리지 않게 격리.
7. **학년도 모델 명시 분리** — `page_year`(접근 시점) vs `result_year`(데이터 학년도) 스키마 분리. `page_year`가 입시 전이면 입결 없는 것이 정상이라는 정책 명문화.
8. **`univInfo.do` API 발견** — 대학명 → unvCd 자동 변환. 사용자가 대학명만 입력해도 OK.
9. **비용 추정 구체적** — 부트스트랩 $9/60대학, 정상 $2/60대학 (~25분 / ~20분). 운영 의사결정에 바로 쓸 수 있는 숫자.
10. **REVIEW 시트 컬럼 정의** — `unvCd | univName | 항목 | 웹사이트 표현 | 추정 매핑 | 신뢰도 | 스크린샷 경로 | 결정 | 사람 수정값 | 비고`. 사람 검수가 실제 어떻게 일어나는지 구체.

### 3.2 약점·누락

1. **수시 단일 시트만 다룸** — 정시·평가기준은 범위 외. **3주차 회의의 데이터랩스 발표("어디가 전형별 주요사항 = 산출방식 같이 수집")는 미반영**. 이지현이 다룬 4시트 구조와 비교 시 절반 미만 커버.
2. **스키마 정의가 본문에 있음** — yaml/json으로 분리 안 됨. 이지현은 `schema_v3.yaml`을 별도 입력 파일로 명시. 운영 시 코드와 스키마가 한 파일에 묶여있으면 버전 관리 약해짐.
3. **8개 대학 사전 분석 없음** — 대학별 이질성(가천 비율%/서울 7등급/연세 8등급/제주 점수/경북 정성종합 등)에 대한 도메인 지식 자료 부재. Mapper가 LLM 호출 시 제공할 컨텍스트가 명시되지 않음.
4. **정규화 사전 별도 없음** — 본문 §3.3에 "사전 자동 업데이트" 언급은 있으나, 초기 사전의 내용·예시 없음. 이지현 `normalization-dictionary.md`의 9섹션 같은 구체 사전이 빠짐.
5. **평가/골든셋 비교 체계 없음** — "스폿 체크: 매 회차 무작위 N건 사람 1:1 대조"만. 정량 평가(PK 매칭률, 셀 일치율) 없음. 이지현 `evaluator` + `evaluation_report.xlsx`의 핵심 가치가 비어있음.
6. **DoD(Definition of Done) 약함** — "신뢰도 평균, LLM 실패율" 트래킹 언급만. 이지현의 "PK 매칭률 ≥ 85% / 셀 일치율 ≥ 90% / seed 50% 채움" 같은 측정 가능한 완료 기준 없음.
7. **확장 단계 약함** — 60개 단위 1회 배치 가정. 8개 → 200+로 가는 단계화, 부분 갱신 모드(특정 대학만 재수집)는 가볍게 언급만.
8. **출력이 단일 시트** — `outputs/{unvCd}.xlsx`에 `수시` 시트 1개. 골든셋 양식(2단 헤더, 29컬럼, 그룹 헤더 N1:T1·U1:AC1 병합)에 대한 일치 보장 없음.
9. **`univDetail.do` 메뉴 클릭 주장은 유찬 POC로 반박됨** — `univDetailSelection.do` 직접 GET이 작동. Playwright 없이도 가능한 부분이 있었음.

---

## 4. 유찬(본인) — W03 산출물 (POC + 메타·통합·인프라 레이어)

> 본인의 도메인 v1 문서 산출물은 미완성이나, **(1) 어디가 전형 결과 POC 크롤러** + **(2) 두 사람의 산출물 위에 통합 분석 + 시스템 컨텍스트 + 의사결정 프레임워크를 얹는 메타 레이어** 두 갈래로 기여.
> 상세: `week03/docs/integration-context.md`

### 4.1 산출물 5종

**⓪ POC 크롤러** — `week03/crawl_adiga.py` (212L). 어디가 전형 결과 탭 추출, `requests + BS4` 정적 크롤링 + `ThreadPoolExecutor` 멀티스레드 배치 처리 (3 워커, 10건 배치, 2초 딜레이). `target_universities.csv` (221개 대학 마스터) 포함. **이지현 분석(정적 HTML, BS4 OK) 입증 + 임지현 URL 주장 일부 반박** (`univDetailSelection.do` 직접 진입, 메뉴 클릭 불필요). 단, 헤더 평면화·운영 가드·전형별 주요사항은 미구현.

**① Notion MCP 인프라** — Claude Code ↔ 조교ON 연동 (Claude Code CLI + Desktop App via mcp-remote 브릿지). 12주 내내 자연어로 조교ON 워크스페이스(루트 + Archive 7 + Milestone W01~W12 + 멤버 페이지)에 즉시 접근. 토큰 효율 약 30배 절약(raw JSON 98KB → markdown 3KB).

**② 프로젝트 CLAUDE.md** — 단일 진실원 standing instructions (164L/7.7KB). 12주 로드맵 + "계획(Notion) vs 실제(로컬)" 피벗 명시 / 두 갈래 처리 아키텍처 / 도메인 용어 / AI 작업 공통 원칙 7개 / Notion 페이지 인덱스 + on-demand fetch 규칙. 새 Claude 세션 진입 시 자동 로드.

**③ 핸드오프 비교 분석** — `week04/docs/handoff-analysis.md` (본 문서). 14영역 × 3인 비교 매트릭스 + MVP 통합 경로 + 충돌 의사결정 4건 + 누락 영역 7개 발견.

**④ W04 실행 액션 플랜** — §7의 6단계 일정 + DoD(MVP) + 충돌점 권장안 + W05+ 확장 단계화.

### 4.2 자가 진단 (한계)

1. **도메인 v1 문서 산출물 부재** — `crawl-design-spec.md`, `target-universities.md` 등 week03/docs/ 템플릿 4종이 빈 상태. POC 코드로 일부 보완되나 문서화된 설계서는 없음.
2. **POC 깊이 부족** — `crawl_adiga.py` 212L로 전형 결과 추출은 작동 확인했으나, 헤더 평면화·부트스트랩·빈 산출 가드 같은 임지현 POC가 다룬 운영 디테일은 미구현. 221개 대학 배치 실제 실행 결과도 아직 없음.
3. **전형별 주요사항(B 트랙) 미진행** — 두 갈래 처리 구조의 절반만 다룸.
4. **회의 합의 전** — MVP 경로·충돌 의사결정 4건은 본인 권장안. 3인 합의 필요.

### 4.3 W04에서 본인 역할

1. **MVP 구현 주도** — §5.1 경로 코드 작성 (가천대 → 8건 → 골든셋 평가). 기존 `crawl_adiga.py`에 헤더 평면화·빈 산출 가드·전형별 주요사항 트랙을 추가.
2. **통합 산출물 v2 발전** — 회의 결과·구현 발견 반영해 W04 산출물로 흡수
3. **검수 회로 운영** — 이지현 ↔ 유찬 사이클 (`quality-criteria.md` 기준)
4. **누락 영역 보강** — §6의 7개 중 W04 필요한 것 우선 (로깅, PII)

---

## 5. 통합 제안 — W04 구현 시 어떻게 합칠 것인가

W04 목표가 "굴러가게만"이므로, **두 설계서를 그대로 통합하면 과욕**이다. 우선순위 기반으로 **최소 구현(MVP) → 확장 단계** 두 트랙으로 나눈다. 본인 `crawl_adiga.py` 212L이 이미 정적 HTML+BS4 기반으로 작동하므로 이걸 토대로 운영 디테일을 얹는 방향.

### 5.1 MVP (W04 안에) — 본인 POC + 임지현 운영 디테일 + 이지현 자산

| 영역 | 채택 | 출처 | 이유 |
|---|---|---|---|
| **베이스 크롤러** | `crawl_adiga.py` 212L (requests+BS4 정적, 멀티스레드 배치) | 본인 POC | 이미 작동 확인됨, 임지현 Playwright 불필요 입증 |
| **데이터 범위** | 수시 결과(susi_result) **단일 시트만** | 임지현 | 1주차 안에 굴러가게 하는 목표 |
| **대상 대학** | 이지현 8개 대학 (가천·서울·제주·연세·고려·부산·경북·남서울) | 이지현 + 골든셋 | 골든셋 평가가 가능한 표본 (본인 221대학 마스터의 부분집합) |
| **URL/진입 흐름** | `univDetailSelection.do?menuId=PCUVTINF2000&unvCd={code}&searchSyr={year}` 직접 GET | 본인 POC 실측 (이지현 분석 일치) | 메뉴 클릭 불필요 확인됨 |
| **표 파싱** | 본인 단순 td/th 추출 + **임지현 헤더 평면화 추가** | 본인 POC + 임지현 §3.2 | MVP는 단순으로 작동, 품질 향상은 평면화 추가 |
| **부트스트랩 모드** | 매핑 사전 빈 상태에서 시작, 첫 5개 강제 REVIEW | 임지현 §3.3 | 콜드 스타트 보호 |
| **에러 분류** | fatal(인증·잔액) 즉시 중단 + transient 백오프 + 표 단위 격리 | 임지현 §3.3 | 비용·시간 낭비 방지 |
| **빈 산출 가드** | records=0 / <30% / admission_type 한쪽만 → REVIEW | 임지현 §3.4 | 침묵 실패 방지 |
| **정규화 사전** | 이지현 `normalization-dictionary.md` 9섹션을 **시드**로 사용 | 이지현 | 즉시 활용 가능한 자산 |
| **8대학 도메인 지식** | `00_통합_발견.md` + 04~07 분석 노트 | 이지현 | LLM 프롬프트 컨텍스트 |
| **학년도 모델** | `page_year` vs `result_year` 분리 | 임지현 | 향후 혼동 방지 |
| **출력 양식** | 골든셋 2단 헤더 양식 일치(29컬럼) | 이지현 | 평가 자동화 가능 |
| **골든셋 평가** | susi_result 한정 PK 매칭률·셀 일치율 측정 | 이지현 `evaluator` | DoD 수치화 |

### 5.2 W05+ 확장 (MVP 통과 후)

- 평가기준 시트(susi_eval, jeongsi_eval) 추가 — 이지현 4시트 구조
- 정시 시트(jeongsi_result) 추가
- 전형별 주요사항(B 트랙) 통 크롤링 시작
- 8개 → 221개 대학 확장 + 부분 갱신 모드 — 이지현 `run_state.json` + 임지현 `--refresh`
- 신규 칼럼 후보 제안(T4c, Opus 1회) — 이지현
- LLM 자동 사전 업데이트 + history 백업 — 임지현

### 5.3 두 설계서를 그대로 합치지 못하는 부분 (의사결정 필요)

| 충돌 지점 | 이지현 | 임지현 | 권장 |
|---|---|---|---|
| 기술 스택 | requests + BS4 (정적 HTML 확인됨) | **Playwright** (메뉴 클릭 필요) | **이지현 채택** — 본인 POC로 `univDetailSelection.do` 직접 GET 작동 확인됨, Playwright 불필요 |
| 출력 시트 구조 | 4시트 (수시/정시 × 결과/평가) | 1시트 (수시) | MVP는 임지현. W05+ 4시트로 확장. |
| LLM 모델 | Haiku→Sonnet→Opus 에스컬레이션 | Sonnet 4.6 단일 | 임지현(단순). 비용 관찰 후 에스컬레이션은 W06+에서 도입. |
| 스킬/에이전트 분리 | 6 스킬 + 2 서브에이전트 | 5 에이전트 (Orchestrator + 4) | MVP는 단순. **이지현의 분리는 W05+ 리팩터링**으로 미룸. 4주차 굴러가게만 단계에서 SKILL.md/AGENT.md 파일 다 만드는 건 오버엔지니어링. |

---

## 6. 누락 영역 통합 정리

세 사람 어디에도 안 잡힌 부분 (W04~W12 어디선가 다뤄야 함):

1. **로깅·관측 가능성** — 두 설계 모두 콘솔 로그 정도. 본인 POC도 print 위주. 구조화 로그(JSON 라인) / 메트릭 / 알림 채널 부재.
2. **테스트** — 단위 테스트, 회귀 테스트 정책 없음. 매핑 사전·평면화 같은 결정론 부분은 테스트 작성 가능한데 명세 없음.
3. **adiga 이용 정책·법적 검토** — 임지현이 "범위 외" 명시. 이지현도 언급 없음. 본인 POC는 robots.txt 확인 체크박스가 미체크. 실제 221개 대학 배치 실행 전에 한 번 확인 필요.
4. **DB 연동 (W07~W08 계획)** — 두 설계 모두 엑셀까지만. Notion Kick-off에 W08 "DB 입력 자동화" 있는데 두 설계서는 DB 스키마 매핑까지 안 감. 본인 POC는 MariaDB 저장 옵션은 있으나 스키마 미정.
5. **사람 검수 워크플로우** — REVIEW 시트는 양쪽 있으나, **실제 사람이 검수한 결과가 사전에 어떻게 반영되는지**의 사이클 명세는 임지현만 부분적.
6. **재현성·결정성** — LLM 비결정성 흡수는 두 설계 모두 confidence 점수에 의존. 동일 입력 → 동일 출력 보장은 없음. 운영 중 "같은 페이지인데 다른 결과"가 나오면 어떻게 디버깅할지 약함.
7. **PII / 마스킹** — 어디가 데이터는 통계라 거의 없으나, 모집요강 PDF·이미지에는 담당자 연락처·합격 사례가 들어갈 수 있음. 양쪽 다 다루지 않음.

---

## 7. 권장 액션 (유찬용)

W04 시작 시 다음 순서로 진행 권장:

1. **(반나절)** 본 분석을 양쪽에 공유, 위 §5.1 MVP 범위를 3인 합의로 확정. PRD 갱신.
2. **(0.5일)** 본인 `crawl_adiga.py`를 베이스로 임지현 §3.2~§3.4 운영 디테일(헤더 평면화·빈 산출 가드·fatal/transient) 추가. 이지현의 자산(정규화 사전, 8대학 분석, schema_v3.yaml, 골든셋)을 입력 자료로 첨부.
3. **(1일)** 1개 대학(가천대 권장 — 골든셋 285행, 이지현 분석 노트도 풍부) end-to-end 굴리기. 헤더 평면화 + 부트스트랩 모드 + 빈 산출 가드 작동 확인.
4. **(1일)** 8개 대학 확장 + 골든셋 평가 첫 실행. PK 매칭률·셀 일치율 측정.
5. **(0.5일)** 부적합 사례 분류 A/B/C로 기록 → W05 입력.
6. **(상시)** 유찬의 빈 week03 문서 4종을 채워 넣을지, 본 분석을 W04 산출물로 흡수시킬지 결정. **본 분석이 사실상 v2 설계 통합 문서이므로 week03 v1 채우기는 생략하고 본 문서를 단일 진실원으로 두는 것을 권장.**

---

## 8. 결론

- **이지현 = 범위·도메인 지식·평가체계의 베이스라인**
- **임지현 = POC 검증된 운영 디테일 + 어디가 사이트 일부 가설(메뉴 클릭 등) 재검토 필요**
- **유찬 = 어디가 전형 결과 POC(212L) + 두 산출물의 통합 + 본인 구현 + 검수 회로 운영**

W04 안에는 본인 `crawl_adiga.py` 위에 임지현 운영 디테일(평면화·부트스트랩·격리·가드)을 얹고 이지현 자산(8대학 분석·정규화 사전·골든셋·평가기준)을 입력 자료로 활용해 **가천대 1건 → 8개 대학 → 골든셋 평가**까지 도달하는 것이 가장 현실적 목표.

확장(4시트, 221+ 대학, DB 연동)은 W05~W08에서 단계화.
